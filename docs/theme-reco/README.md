---
title: git
date: 2020-05-29
---

## git 使用

```
git add :添加文件到缓存区
git commit :提交文件到分支，-m添加说明
git status :查看文件是否有修改
git diff :对比文件修改内容
git rest --hard HEAD^ :回到上一个版本
git rest --hard xxx :回到指定版本，版本号可以不写全
git reflog :查看历史提交版本
git rm :删除版本库文件
rm :删除本地文件
ls :查看文件目录
git merge --no-ff -m '':合并分支

```
